# scripts

Chứa utility deterministic dùng cho build/demo, ví dụ:
- canonicalize + hash credential;
- seed demo data;
- verify fixture integrity;
- export Devnet proof metadata.

Không đặt business logic chính hoặc secret/API key trong scripts.
